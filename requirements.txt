# ── requirements.txt ──────────────────────────────────────────────────────────
# Safe to install on macOS Python 3.14 and Ubuntu server.
# GPU / torch / onnxruntime packages are in requirements-server.txt
# ─────────────────────────────────────────────────────────────────────────────

# LLM & Agent
langchain==0.3.25
langchain-community==0.3.24
langchain-ollama
langchain-huggingface
langgraph==0.4.1
langchain-text-splitters==0.3.8

# Sparse retrieval
rank_bm25

# Intent classifier — tokenizer and inference (torch loaded lazily at runtime)
transformers

# Evaluation
scikit-learn
jiwer==3.0.5

# Data ingestion — pure-Python / pre-built wheels
requests==2.32.3
beautifulsoup4==4.13.4
lxml==5.4.0
pypdf==5.4.0

# API backend
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# Telegram bot
python-telegram-bot==22.0

# Web UI
gradio==5.29.0

# Utilities
python-dotenv==1.1.0
httpx==0.28.1
aiofiles==24.1.0
pydantic==2.11.3
pyyaml
